<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/style.css" />
    <title><%= gameName %></title>
</head>
<body>
    <h1><%= gameName %></h1>
    <div id="game-info">
        <h3>Players</h3>
        <p><%= minPlayers %> to <%= maxPlayers %></p>
        <h3>Play Time</h3>
        <p><%= playTime %> Minutes</p>
        <h3>Description</h3>
        <p><%= aboutGame %></p>
        <h3>Beginner Friendly?</h3>
        <% if (beginnerFriendly) { %>
        <p>Yes</p>
        <% } else { %>
        <p>No</p>
        <% } %>
    </div>
    <div class="button-box">
        <form action="/games/<%= _id %>/edit">
            <button type="submit">Edit/Delete Game</button>
        </form>
        <form action="/games">
            <button type="submit">Back To Hoard</button>
        </form>
    </div>
</body>
</html>

<!-- 
SCHEMA REFERENCE:

// defining schema for each board game
const gameSchema = new mongoose.Schema({
	gameName: String,
	minPlayers: Number,
	maxPlayers: Number,
	playTime: Number,
    aboutGame: String,
	beginnerFriendly: Boolean,
});

-->